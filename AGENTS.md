# 广宣法务审核 Agent 项目说明

## 1. 项目目标与 Agent 工作原则

本项目要建设一个面向企业法务、品牌、市场和设计团队使用的「广宣法务审核 Agent」，用于审核广告图片、宣传海报、宣传视频、宣传语、PPT、PDF、Word 等正式对外传播的营销物料。产品不以完全替代法务为目标，而是让 AI 承担材料解析、全量初筛、风险识别、规则检索、风险定位、修改建议生成、版本差异比较和复审辅助等重复工作，让法务把精力集中在争议问题、专业判断和最终审批上。

开发 Agent 在分析需求、设计页面、编写代码和调整流程时，必须始终遵守以下产品边界：AI 可以阅读、检索、归纳、定位、解释和辅助判断，但不得冒充法务给出不可撤销的最终法律结论；所有正式宣传物料的最终批准必须由有权限的法务人员完成。系统输出应当明确区分 AI 判断与人工结论，并完整保留人工修改、审批和签名记录。

整个产品由「AI 法务助手、接收区、反馈区、终审区」四个核心模块组成。判断一个功能应当放在哪个模块时，应使用以下原则：AI 法务助手处理零散问题，接收区承接正式任务，反馈区完成第一次全量初审，终审区负责已经确认的风险整改闭环。不得为了页面丰富而在多个模块重复实现相同的核心流程。

## 2. 核心用户与典型工作方式

主要用户是企业法务人员。法务负责设置审核关注点、确认 AI 识别出的风险、处理争议问题、提出正式修改意见、完成终审和签名。品牌、市场、设计或业务人员可以提交物料、查看与自己有关的整改意见、上传修改版本，但不能代替法务关闭风险或批准最终上线版本。管理员负责维护权限、知识库来源、企业规则、产品参数和系统审计配置。

用户遇到一句临时宣传语或一个零散法律问题时，应直接使用 AI 法务助手；需要正式审核一批宣传材料时，应在接收区创建审核任务并上传物料；AI 完成第一次全量扫描后，应在反馈区集中呈现通过材料、待人工判断材料和明确风险材料；经法务确认需要整改的 Risk Case，应进入终审区持续跟踪版本、复审和审批，直至正式关闭。

## 3. AI 法务助手

AI 法务助手是轻量、即时的咨询入口，核心职责是「咨询与单条分析」。用户可以输入一句宣传语、一段营销文案，或者临时上传一张图片、一段视频或一个小型文件，询问内容是否存在明显风险、涉及哪些法律规则、为什么被判定为高风险、是否有类似历史案例、需要准备什么证明材料，以及如何改写为风险更低的表达。

助手应结合当前输入、法律法规知识库、企业内部广宣审核规范、产品真实参数、Approved Claims 和历史审核案例进行回答。输出应尽量包含风险点、风险原因、风险等级、适用的规则或法律依据、类似案例、修改建议、推荐表达和需要补充的证明材料。引用法规、企业规则或历史案例时，应展示可追溯的来源；没有充分依据时应明确说明不确定性，不得虚构法条、案例、产品参数或证明材料。

AI 法务助手也可以作为其他页面中的 Copilot，帮助法务解释当前 Risk Case 或生成便于与市场、设计沟通的话术，但它不能创建正式审核任务，不能对几十份材料执行批次审核，不能生成正式的批次通过率，不能直接改变 Risk Case 状态，也不能执行终审、法务签名、最终批准或风险关闭。如果用户希望把临时分析转成正式审核，应提供「转为正式审核任务」入口，并把输入材料和已填写信息带入接收区，由用户确认后再创建任务。

## 4. 接收区

接收区是正式审核流程的唯一入口，核心职责是创建审核任务、接收宣传材料、完成机器解析并记录本次审核要求。用户可以创建一个审核任务 Case，例如“2026 秋季新品上市宣传物料审核”“XX 车型发布会广宣审核”或“9 月社交媒体宣传内容审核”，并在任务下批量上传图片、视频、文案、PPT、PDF、Word 等不同类型的材料。

系统应记录任务名称、任务编号、创建时间、提交人、所属项目、负责人、物料数量、物料类型、审核截止时间和当前状态。上传后，系统先把不同格式的物料转成统一、可审核的内容：图片通过 OCR 提取文字并通过视觉模型理解画面；视频通过 ASR 提取口播、OCR 提取字幕，并对关键帧和画面语义进行理解；PPT、PDF 和 Word 需要解析文字、图片、页码、段落和结构。解析结果必须保留与原文件位置的映射，保证后续风险可以回到具体图片区域、视频时间点、文档页面或段落。

接收区应提供审核要求区域，允许法务输入本批任务的额外关注点，例如重点检查绝对化宣传、安全承诺、没有依据的数据、竞品相关表达、价格宣传、免责声明以及可能误导消费者的描述。系统可以提供常用审核要求模板，但不得把模板内容默认为本次任务的正式要求，用户选择或确认后才生效。

接收区只负责把任务和材料规范地送入审核系统，不在该页面提前展示最终风险结论，不生成正式风险报告，不处理整改版本，也不执行审批。完成上传、解析和审核要求设置后，用户点击「开始 AI 初审」，任务进入反馈区。若材料解析失败、文件受损或缺少必要信息，应阻止该材料进入正式初审，并明确告诉用户需要重新上传或补充什么。

## 5. 反馈区

反馈区负责一批正式宣传材料的第一次 AI 全量初审，核心职责是发现风险、分类风险、定位风险并告诉法务哪些内容需要重点处理。它回答的是“这一批材料第一次审核发现了什么”，而不是“这些风险最后是否已经解决”。

初审完成后，系统应把物料分为三类。第一类是「初审通过」，表示 AI 当前没有发现明显风险，但必须注明这并不等同于法务最终批准；第二类是「待人工判断」，表示存在疑点，但由于证明材料不足、事实无法核实、法律解释存在空间、企业历史规则冲突或需要专业判断，AI 无法独立给出确定结论；第三类是「风险未通过」，表示内容命中了较明确的风险规则，在修改或补充证明材料前不能进入批准状态。

反馈区不能只在文件层面显示通过或不通过，而必须把每一个独立问题建立为 Risk Case。例如一张海报同时出现“行业第一”“100% 保障安全”和“续航 1000km 但未标注数据来源”时，应创建三个可分别确认、误判、整改和关闭的风险记录。每个 Risk Case 至少包含风险编号、所属任务、所属物料及版本、风险原文或画面内容、风险位置、风险类型、风险等级、风险原因、置信度、相关企业规则、相关法律依据、证据来源、AI 修改建议、推荐表达、所需证明材料和当前状态。

风险定位必须与物料类型相匹配。图片风险应支持框选文字或画面区域，视频风险应定位到开始和结束时间并展示对应字幕或关键帧，文档风险应定位到页码、段落或句子。法务进入风险详情后，应能直接跳转到原内容所在位置，而不需要从头重新阅读整份材料。

法务在反馈区可以对 AI 结果执行「确认风险」「标记误判」「要求补充材料」或「转入整改」等操作。被确认需要修改的 Risk Case 自动进入终审区；被标记为误判的记录不能删除，必须保存法务理由，作为后续模型评测和规则优化数据；待人工判断的风险只有在法务作出处理后才能继续流转。反馈区不得管理 V1、V2、V3 等整改版本，不负责 Version Diff、最终审批、签名或风险关闭。

## 6. AI 初审报告

反馈区应自动生成当前审核任务的 Markdown 格式初审报告，报告只反映第一次 AI 初审结果。报告需要包含本批物料总数、初审通过数量、待人工判断数量、风险未通过数量、物料层初审通过率，以及高、中、低风险项数量。通过率的计算口径必须在界面中明确，不能把 Risk Case 数量和物料数量混为一谈。

报告还应汇总风险类型分布，例如绝对化宣传、产品性能缺乏证明、安全承诺、竞品比较依据不足、价格宣传、免责声明缺失和可能误导消费者的描述。每一个待人工判断或风险未通过的 Risk Case 都应展示风险物料、风险原文、风险位置、风险等级、风险原因、审核依据、证据来源、修改建议和推荐表达。

每个风险下应自动生成一段可以发送给设计、品牌、市场或业务团队的沟通话术。沟通话术应当清晰、专业、可执行，说明问题是什么、为什么需要修改、建议如何修改，以及保留原表达需要补充哪些材料。报告允许复制和导出，但报告中的 AI 结论不得伪装成法务已经批准的正式意见。

## 7. 终审区

终审区只管理反馈区已经发现并经法务确认需要处理的 Risk Case，核心职责是风险整改、版本管理、差异比较、AI 复审、法务终审、签名、关闭和审计追溯。这里的主要对象是风险记录，而不是重新对所有物料执行一次完整初审。

市场、品牌、设计或业务人员完成修改后，应把新文件上传到原物料和对应 Risk Case 下，系统按照 V1、V2、V3 等方式保留连续版本。系统不得使用新文件覆盖旧版本，也不得因为重新上传而删除原 Risk Case。每一个版本都要记录上传人、上传时间、修改说明、文件哈希和与上个版本的关联关系。

终审区应提供 Version Diff。对于文字内容，展示删除、替换和新增表达；对于图片，展示文字、画面和版式变化，并关联风险位置；对于视频，展示字幕、口播、镜头或关键帧变化；对于文档，展示页面和段落变化。AI 复审必须优先回答三个问题：原风险是否已解决，当前是否仍有剩余风险，本次修改是否产生了新的风险。为了防止局部修改引入上下文风险，系统可以对变化区域及必要上下文进行扩展检查，但不得把终审区重新做成一次无差别的全量初审。

终审结果应清楚展示原风险、原版本内容、新版本内容、差异、AI 复审判断、证据和置信度。若原风险已解决但发现新增风险，应新建关联的 Risk Case，而不是悄悄改变原风险的含义。只有 AI 复审通过、法务人工确认并完成有权限的法务签名后，Risk Case 才能进入 Closed 状态；只有一份物料关联的所有阻断性风险都关闭后，该物料版本才可以被标记为最终批准版本。

## 8. 状态流转与权限约束

审核任务建议至少包含 Draft、Parsing、Ready for Review、AI Reviewing、Feedback Pending、Remediation、Final Review、Approved、Rejected 和 Archived 等状态。Risk Case 建议使用 Open、Pending Legal Decision、Confirmed、Awaiting Evidence、Awaiting Revision、Resubmitted、AI Re-review、Legal Final Review、Closed、Rejected as False Positive 等状态。实现时可以根据技术框架调整枚举名称，但不得省略“待人工判断、整改中、已重新提交、AI 复审、法务终审、已关闭、误判留痕”等关键语义。

状态变更必须由明确事件触发并写入 Review Record。AI 可以建议状态或完成 AI 阶段的状态流转，但无权代替法务执行最终批准、法务签名或风险关闭。前端按钮、后端接口和数据库权限都必须遵守同一套授权规则，不能只在界面上隐藏按钮。所有关键状态变更都应记录操作者、时间、原状态、新状态、原因和关联版本。

## 9. 核心数据模型

核心关系为「审核任务 Case → 宣传物料 Material → 物料版本 Version → 风险 Risk Case → 审核记录 Review Record」。一个 Case 可以包含多个 Material；一个 Material 必须保留一个或多个不可覆盖的 Version；一个 Version 可以产生多个 Risk Case；一个 Risk Case 可以跨越多个整改版本，但必须始终关联首次发现它的版本；每一次 AI 判断、人工意见、状态变化、签名和关闭操作都应形成 Review Record。

Case 至少应保存任务信息、提交信息、项目归属、审核要求、负责人、截止时间和聚合状态。Material 至少应保存文件类型、原始文件、解析状态、解析内容和位置映射。Version 至少应保存版本号、文件哈希、上传人、上传时间、修改说明和父版本。Risk Case 至少应保存风险类型、风险等级、风险原文、风险位置、原因、依据、证据、置信度、建议和状态。Review Record 至少应保存执行主体是 AI 还是人工、执行动作、判断结果、意见、时间、关联版本和状态变化。

不得使用删除历史记录的方式表示风险已经解决。风险解决应表现为 Risk Case 状态进入 Closed，并保留完整的版本链和审核记录。对外批准的文件必须能反向追溯到批准时使用的准确版本，不能只关联到一个可能继续被覆盖的文件地址。

## 10. Legal Knowledge Base

系统底层需要建立统一的 Legal Knowledge Base，为 AI 法务助手、反馈区和终审区提供一致的知识支持。知识内容包括法律法规、司法或监管解释、企业内部广宣规范、产品真实参数、第三方证明材料、历史审核案例、Approved Claims、历史驳回表达、Risk Case 和法务最终结论。

每条知识应尽量保存来源、适用地区、适用业务或产品、发布日期、生效日期、失效日期、版本、维护人和可信级别。检索结果必须区分现行规则、历史规则、企业内部规则和历史案例。历史通过案例不能直接视为当前场景必然通过，Approved Claim 也必须同时检查适用车型、时间、渠道、数据来源、统计口径和必需免责声明。

法务对 AI 结果的确认、修改和误判标记可以沉淀为后续优化数据，但不得在未经治理的情况下直接写回高可信法律法规库。法规更新、企业规则变更和产品参数更新应有版本管理和审批机制，避免旧知识继续影响新任务。

## 11. AI 审核与输出要求

AI 审核应采用“内容解析、候选风险识别、规则与证据检索、风险判断、结构化输出”的方式执行。任何高风险结论都应尽可能关联具体原文、位置和依据。模型无法获取关键事实、证明材料或适用规则时，应输出待人工判断或要求补充材料，而不是为了完成流程编造确定答案。

结构化结果必须稳定、可校验并能够持久化。至少应包含 material_id、version_id、risk_type、risk_level、risk_text、location、reason、confidence、rule_references、evidence_references、suggestion、recommended_copy、required_evidence 和 recommended_status。模型生成的自然语言报告应基于已经校验的结构化结果生成，不能让报告和数据库中的 Risk Case 数量或状态互相矛盾。

风险等级、置信度和审核状态是三个不同维度。风险等级描述潜在影响，置信度描述 AI 对识别结果的把握，审核状态描述流程所处阶段，界面和数据模型不得把三者混用。对于低置信度但潜在影响较高的问题，应优先进入人工判断，而不是简单忽略。

## 12. Audit Trail、合规与安全

系统必须永久或按照企业合规策略保留最初提交版本、最初风险、第一次 AI 判断、法务意见、每个修改版本、每次 AI 复审、每次人工复审、最终批准人、批准时间和最终上线版本，形成完整 Audit Trail。所有导出报告也应记录生成时间、数据版本和生成者，保证未来发生争议时可以还原当时的真实审核过程。

宣传材料可能包含未发布产品、商业秘密和个人信息，因此文件存储、日志、模型调用、知识库检索和导出都必须执行访问控制。不得把敏感原文完整写入不受控日志；不得在没有权限的情况下跨项目检索历史材料；需要调用外部模型或第三方服务时，应遵守项目的数据安全配置和脱敏规则。

## 13. 页面与交互设计约束

页面设计应优先帮助法务快速定位和处理风险，而不是堆叠大段 AI 文本。接收区突出任务信息、上传进度、解析结果和审核要求；反馈区突出物料分类、风险定位、筛选、人工判断和初审报告；终审区突出 Risk Case 状态、责任人、版本时间线、Version Diff、复审结果和签名操作；AI 法务助手保持轻量，并能携带当前页面上下文回答问题。

相同概念在不同页面必须使用一致的名称、颜色和状态语义。风险等级与流程状态应使用不同的视觉编码。任何“通过”文案都必须说明是 AI 初审通过、AI 复审通过还是法务最终批准，禁止只显示含义模糊的“已通过”。高风险动作，例如最终批准、驳回、关闭风险和撤销批准，需要二次确认并展示影响范围。

## 14. 开发实施规则

开发前应先阅读现有仓库结构、技术栈、数据库模型和已有组件，尽量复用项目现有设计系统和工程约定。未经用户要求，不要因为实现本需求而重写无关模块、替换技术栈或大规模重构。若需求与现有实现冲突，应优先指出冲突、说明影响，并选择改动范围最小且能够保留数据一致性的方案。

实现顺序应优先保证端到端主链路可运行：创建 Case 与上传 Material，完成解析并保留位置映射，执行首次 AI 初审并生成 Risk Case，在反馈区完成人工判断，把确认风险转入终审区，上传新 Version，生成 Version Diff 和 AI 复审结果，最后由法务确认、签名并关闭风险。知识库增强、统计看板和体验优化可以在主链路稳定后迭代。

所有 AI 输出都必须经过 Schema 校验、异常处理和可重试设计。耗时的 OCR、ASR、视频理解和批量审核应使用异步任务，并向用户展示真实进度、失败材料和重试入口。系统需要避免同一文件或同一任务因重复提交而创建重复 Risk Case，可通过文件哈希、任务幂等键和规则命中去重策略实现。

## 15. 测试与验收标准

每次新增或修改功能后，至少验证模块边界、权限、状态流转、数据留痕和异常路径。测试样例应覆盖图片、视频、PPT、PDF、Word 和纯文本，覆盖单文件多风险、多个文件同类风险、缺少证明材料、AI 误判、解析失败、重复提交、版本无变化、原风险已解决但出现新增风险、并发修改和无权限审批等情况。

主链路验收时，必须能够从一个新建审核任务开始，完整走通上传材料、解析、AI 初审、生成 Risk Case、法务确认、进入整改、上传 V2、展示 Version Diff、AI 复审、法务终审、签名、关闭风险和标记最终批准版本。完成后应能从最终版本反查全部风险和审核记录，也能从任一 Risk Case 找到最初物料、各次修改版本和最终批准信息。

AI 效果评估不能只看整体准确率，还应分别关注高风险召回率、误报率、风险定位准确率、法规依据有效率、修改建议可执行性、待人工判断比例和法务实际节省时间。涉及高风险漏检的测试应优先于文案风格优化。任何尚未实现或尚未验证的能力，都应明确标记为待办或实验能力，不得在产品界面中伪装成已经可靠可用。

## 16. 最终产品边界

本项目的最终工作方式应保持稳定：AI 法务助手解决临时问题和单条咨询；接收区负责正式审核任务、物料和审核要求进入系统；反馈区负责第一次全量初审、风险定位和人工确认；终审区负责已经确认的风险整改、版本比较、复审、签名和关闭。底层知识库为各模块提供统一依据，核心数据结构保证所有任务、物料、版本、风险和审核记录可追溯。

如果后续需求会导致两个模块重新承担同一核心职责，开发 Agent 不应直接照做，而应先指出重复点，并按照“助手管问题、接收管任务、反馈管初审、终审管风险闭环”的原则提出归属建议。产品成功的标准不是 AI 生成了多少内容，而是法务是否能够更快发现真正重要的问题、更少重复阅读材料，并在不牺牲责任边界和审计完整性的前提下完成最终决策。
